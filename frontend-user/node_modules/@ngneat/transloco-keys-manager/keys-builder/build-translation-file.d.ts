import { Config, Translation } from '../types';
export interface FileAction {
    path: string;
    type: 'new' | 'modified';
}
interface BuildTranslationOptions extends Pick<Config, 'fileFormat'>, Partial<Pick<Config, 'replace'>> {
    path: string;
    translation?: Translation;
}
export declare function buildTranslationFile({ path, translation, replace, fileFormat, }: BuildTranslationOptions): FileAction;
export {};
