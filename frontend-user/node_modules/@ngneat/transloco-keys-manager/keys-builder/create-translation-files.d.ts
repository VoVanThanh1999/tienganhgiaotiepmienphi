import { Config, ScopeMap } from '../types';
export declare function createTranslationFiles({ scopeToKeys, langs, output, replace, scopes, fileFormat, }: Config & {
    scopeToKeys: ScopeMap;
}): void;
