import { CacheData, CacheHandler, ISROptions } from '../models';
export interface FileSystemCacheOptions {
    cacheFolderPath: string;
    prerenderedPagesPath?: string;
    addPrerenderedPagesToCache?: boolean;
}
export declare class FileSystemCacheHandler implements CacheHandler {
    options: FileSystemCacheOptions;
    protected cache: Map<string, CacheData>;
    private get cacheFolderPath();
    constructor(options: FileSystemCacheOptions);
    add(route: string, html: string, options?: ISROptions): Promise<void>;
    get(route: string): Promise<CacheData>;
    has(url: string): Promise<boolean>;
    delete(url: string): Promise<boolean>;
    getAll(): Promise<string[]>;
    private populateCacheFromFilesystem;
    private addPrerenderedPagesToCache;
    private readFromFile;
    private getFileFullPath;
}
